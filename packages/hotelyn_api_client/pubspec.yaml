name: hotelyn_api_client
description: "REST client for the Hotelyn Dart Frog API."

version: 0.1.0+1

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  hotelyn_domain: any
  http: ^1.2.0

dev_dependencies:
  # Pinned to the version bundled with the Flutter SDK (test_api 0.7.10); see #225.
  test: 1.30.0
  very_good_analysis: ^10.0.0
